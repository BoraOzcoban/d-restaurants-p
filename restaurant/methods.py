import pandas as pd
#from .models import Res


#def csv_to_db():
    #df = pd.read_csv("restaurants/TA_restaurants_curated.csv")
    #records = df.to_records()

    #for record in records:
        #rst = Res(
           # Name=record[0],
           # City=record[1]
        #)
       # rst.save()
